node_modules/
dist/
.env
.env.local
.env.*.local
.DS_Store
*.log

# Written by `firebase use` — names your project, so it stays local.
.firebaserc

# Service account keys. Never commit these.
*serviceaccount*.json
*service-account*.json


# Local Claude Code permissions, specific to one machine.
.claude/settings.local.json
